{
  "name": "jen-island",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8765",
    "check": "for f in $(find js -name '*.js'); do node --check $f || exit 1; done",
    "deploy": "npx wrangler deploy"
  }
}
